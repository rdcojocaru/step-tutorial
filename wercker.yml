box: ubuntu
build:
  steps:
    - script:
      name: step one
      code: echo "Step One"
    - script:
      name: step two
      code: | 
      echo "Step Two"
      echo "python version $(python --version)"
    
